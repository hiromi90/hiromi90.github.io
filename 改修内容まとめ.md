# 改修内容まとめ（要件変更・サイト構成の更新）

本ドキュメントは、今回の改修で追加・変更した要件と、それに伴うサイト構成の
変更点をまとめたものです。既存の開発要件書・サイト構成書に反映してください。

---

## 1. 今回の改修で確定した要件

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加1 | 部員紹介 | 部員カードをクリックすると中央に拡大表示し、クリック時のみ「出身県・学科」を追加表示 | ✅ |
| 追加2 | 大会結果 | 大会結果を GAS 方式に変更。**年度ごとに1つのGASをデプロイ**。シート（タブ）1枚＝1大会。タブ名＝「大会名 開催日｜開催場所」 | ✅ |
| 追加3 | ニュース | 記事作成を非プログラマ向けに。記事内容と写真配置を書いた2つの.txtと画像・ひな形をClaudeに渡し、Claudeが記事ページ完成＋一覧へ1件追加 | ✅ |
| 追加4 | サイト全体 | HTMLを編集せずに更新可能に。**設定ファイル（.txt）方式**。対象：監督紹介・ヒーロー文言・スローガン・SNS/連絡先・部員紹介・駅伝歴代最高記録（2大会とも） | ✅ |
| 改善1 | 記録取得 | 自己ベスト記録ページの 500／CORS エラーを修正 | ✅ |

---

## 2. 設計方針（重要な決定事項）

- **編集用データは .js ではなく .txt に統一**した。
  理由：`.js` はカンマ・引用符の1文字ミスでサイト全体が壊れるため、
  プログラミング未経験者には不向き。メモ帳で編集できる `.txt` に変更。
- サイトは起動時に `content/*.txt` を `fetch` で読み込み、JavaScriptで描画する。
  → **ローカルではLive Server等、公開はNetlify**（従来と同じ運用）。
  HTMLの直接ダブルクリックでは表示されない旨をREADME冒頭に明記。
- SNSリンクは各フッターのHTMLを編集せず、`content/site.txt` の値で
  **href とアカウント名テキストを実行時に自動差し替え**する方式にした。

---

## 3. サイト構成の変更点

### 追加したファイル
```
content/site.txt / coach.txt / members.txt / ekiden.txt   … 編集用テキスト
content/news-example/（はじめにお読みください.txt / 記事.txt / 配置.txt）… ニュース記入例
js/content-loader.js       … content/*.txt を読み込み各ページに描画
js/results-config.js       … 大会結果の「年度→GAS URL」設定
apps-script/personal.gs    … 自己ベスト用GAS（堅牢版・500を返さない）
apps-script/results.gs     … 大会結果用GAS（年度スプレッドシートの全シートを返す）
```

### 変更したファイル
- `index.html`：ヒーロー・スローガンに `data-site` 属性、監督欄を `#coach-wrap` に、`content-loader.js` 読込追加。
- `members/index.html`：`members-data.js` を廃止し `content-loader.js` に変更（カード＋モーダルは content-loader が描画）。
- `records/results/index.html`：静的サンプルを撤去し、年度ナビ＋結果コンテナ（GAS描画）に変更。
- `records/history/index.html`：駅伝2大会のべた書きHTMLを `#ekiden-container` に置換（`ekiden.txt` から描画）。
- `contact/index.html`：連絡先メール表示を追加（`site.txt` から差し替え）。
- 全ページ：`content-loader.js` を `main.js` の前に読込（フッターSNSの自動差し替えのため）。
- `js/main.js`：部員描画を content-loader へ移管、自己ベスト／ランキング取得に例外処理を追加、
  大会結果（年度別・GAS）描画処理を新規追加。
- `css/style.css`：部員モーダル、クリック誘導表示、記録の読込中／空／エラー表示のCSSを追加。

### 廃止したファイル
- `js/members-data.js` … `content/members.txt` に統合。

---

## 4. GAS（Apps Script）運用メモ

- **personal.gs**：スプレッドシートIDで直接開き、全体を try/catch で保護。
  シートが無い/空でも `[]` を返し、例外は `{error:…}` をJSONで返す（500を出さない）。
  差し替え時は **必ず「新しいバージョン」でデプロイ**し、アクセスを「全員」にする。
- **results.gs**：年度スプレッドシートの全シート（タブ）を
  `[{ sheet:"タブ名", data:[行オブジェクト...] }]` の形で返す。
  「_」始まりのタブは除外。年度ごとにデプロイし、URLを `results-config.js` に登録。

---

## 5. 検証状況

- 部員ページ：カード36枚（長距離19／短距離16／マネージャー1）、クリックでモーダル表示、
  ブロック見出しの人数自動更新を確認。
- トップページ：ヒーロー・監督（経歴3件）・スローガン（金色2字）・SNS（URL/アカウント名）反映を確認。
- 歴代記録：駅伝2大会（8名／6名）描画を確認。
- 大会結果：年度ボタン生成、大会見出しの分解（大会名／日付｜場所）、1位の金メダル＋行強調、
  PB/SBバッジ、データ未入力・年度未設定時の案内表示を確認。
- 記録取得失敗時に、白画面ではなく案内メッセージが出ることを確認。

---

## 6. 今回の改修（UI改善・共通化）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善2 | サイト全体 | ヘッダー・フッターを **`js/layout.js` 1ファイルで一括管理**。各ページは目印の `<div id="site-header">` / `<div id="site-footer">` のみを持ち、実行時に自動挿入する方式に変更 | ✅ |
| 改善3 | フッター | サイトマップのリンクのクリック範囲を **文字幅だけ** に限定（`align-items:flex-start` + `width:fit-content`） | ✅ |
| 改善4 | ヘッダー | スクロールで透明化した際の現在ページ／ホバー表示を、塗り枠から **文字幅の細い下線** に変更し選択範囲を最小化 | ✅ |
| 改善5 | 埋め込み全般 | カレンダー・地図・お問い合わせフォーム・記録3ページ（大会結果／自己ベスト／歴代）に **「表示には少し時間がかかる場合がございます。」** のローディング表示を追加。読み込み完了で自動的に消える方式に統一 | ✅ |

### 設計方針
- **共通化（改善2）**：ドラッグ&ドロップ公開の運用を変えないため、ビルドツールではなく
  既存の `content-loader.js` と同じ「実行時挿入」方式を採用。読み込み順は
  `layout.js → news-data.js → content-loader.js → main.js` とし、
  ハンバーガー・スクロール制御・現在ページ強調・SNS名の自動差し替えが従来通り動作することを確認。
- **ローディング表示（改善5）**：
  - iframe（カレンダー・地図・フォーム）は `.embed-frame[data-embed-loading]` で囲み、
    iframe の `load` で `.is-loaded` を付与してメッセージを消す（10秒のフォールバックあり）。
  - GAS読み込みの記録3ページは、描画前に `.records-loading` を表示し、描画完了時に置き換わる。
  - 従来 歴代記録・自己ベストにあった「常時固定表示」の同文言は、この自動で消える方式へ置換。

### 変更・追加したファイル
```
【追加】js/layout.js                … ヘッダー・フッターの中身（共通・1ファイル管理）
【変更】全11ページのHTML            … ヘッダー/フッターを目印divに置換し layout.js を読込
【変更】css/style.css               … フッターリンクのクリック範囲、スクロール時の下線、
                                       ローディング表示（.embed-frame / .embed-loading / .records-loading）
【変更】js/main.js                  … iframeローディング解除処理を追加、記録3ページの読込中メッセージを統一
【変更】schedule/contact のiframe   … .embed-frame でラップ
【変更】records/history・personal   … 固定キャプションを撤去（JSのローディング表示へ）
```

### 検証状況
- 全11ページで layout.js によるヘッダー／フッター注入・ナビ9項目・モバイルメニュー・目印div消化を確認（jsdom）。
- スケジュール2枠・お問い合わせ1枠：`load` でメッセージが消えることを確認。
- 記録3ページ：初期状態でローディング表示が出ることを確認。
- 全JSファイルの構文チェック（`node --check`）を通過。

---
---

# 改修内容まとめ 第2回（2026-07-02）

前回に続き、以下の8つの改善要望に対応した。既存の開発要件書・
サイト構成書に反映すること。

## 1. 今回の改修で確定した要件

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善1 | 大会結果 | 年度とGAS URLの設定を `js/results-config.js` → **`content/results.txt`** に移行。メモ帳で「年度：URL」を1行足すだけで年度追加が可能に | ✅ |
| 改善2 | 大会結果 | 大会は **シート名の開催日を読み取り、新しい順に自動で上から表示**。タブは右端に足すだけでよい。日付なしの大会は末尾（右のタブが先） | ✅ |
| 改善3 | 部員紹介 | 写真空欄の部員で灰色の人型が表示されないバグを修正（原因：parseKVが空値をすべて複数行ブロック扱いし「写真：」空欄が直後の「出身県：」「学科：」を飲み込み壊れたimgを生成）。ブロック扱いはキー明示指定方式（blockKeys）に変更 | ✅ |
| 改善4 | ヘッダー | 記録ドロップダウンがすぐ消える問題を修正。①リンクとメニューの隙間を透明の帯（::before）でつなぐ ②閉じるとき0.25秒の猶予（猶予中にカーソルが戻れば開いたまま） | ✅ |
| 改善5 | ニュース | 6ヶ月で自動非表示になった記事の**削除リストを表示する担当者用ページ `/news/maintenance/`** を追加（ナビ非掲載・noindex）。あわせてClaudeに整理を依頼する運用をREADMEに明記。※静的サイトのため完全自動削除は不可 | ✅ |
| 改善6 | ニュース | 下書き置き場 **`news/_drafts/`** を新設（サイト非表示）。「記事が公開される条件（①`news/フォルダ名/index.html` に置く ②`content/news.txt` に1件追加）」を `_drafts/このフォルダの使い方.txt` とREADMEに明記 | ✅ |
| 改善7 | ニュース | 記事一覧データを `js/news-data.js` → **`content/news.txt`**（「---」区切りブロック）に移行。書く場所は自由で自動的に日付の新しい順に並ぶ。記入用の空テンプレ `content/news-new/（記事.txt・配置.txt）` も追加 | ✅ |
| 改善8 | GAS | `results.gs` の SPREADSHEET_ID の説明を明確化：**スプレッドシートから「拡張機能→Apps Script」で開いた場合は空欄のままで正しい**。IDが必要なのは単独スクリプトの場合のみで、URLの `/d/` と `/edit` の間の文字列を貼る | ✅ |

## 2. サイト構成の変更点

### 追加したファイル
```
content/news.txt                 … ニュース一覧データ（旧 js/news-data.js）
content/results.txt              … 大会結果の年度→GAS URL設定（旧 js/results-config.js）
content/news-new/記事.txt         … 記事作成の記入用テンプレ（空）
content/news-new/配置.txt         … 写真配置の記入用テンプレ（空）
news/_drafts/このフォルダの使い方.txt … 下書き運用と公開手順の説明
news/maintenance/index.html      … ニュース整理ページ（担当者用）
```

### 変更したファイル
- `js/content-loader.js`：
  - parseKV/parseRecords に blockKeys 引数を追加（改善3のバグ修正）
  - memberAvatar を文字列チェック＋onerror フォールバック付きに強化
  - news.txt の読み込み・描画（トップ最新5件／ニュースページ6ヶ月フィルタ／整理ページの期限切れリスト）を追加。日付順の自動ソート対応
- `js/main.js`：
  - 旧 NEWS_DATA 依存のニュース描画（10・11節）を削除（content-loaderへ移管）
  - 大会結果（14節）を content/results.txt 読込に変更。年度は自動で降順ソート。大会はシート名の開催日で新しい順に自動ソート（sortSheetsByDate）
- `css/style.css`：ドロップダウンのホバー改善（::before ブリッジ＋閉鎖遅延）、整理ページ用スタイル（.maint-*）を追加
- `index.html` / `news/index.html`：news-data.js の読込を削除（news/index.html は content-loader.js に置換）
- `records/results/index.html`：results-config.js の読込を削除
- `news-template.html` / `content/news-example/はじめにお読みください.txt` / `README.md`：news-data.js への参照を content/news.txt に更新。公開手順・整理手順を追記
- `apps-script/results.gs`：SPREADSHEET_ID の説明を書き直し（空欄でOKの旨を明記）

### 廃止したファイル
- `js/news-data.js` … `content/news.txt` に統合
- `js/results-config.js` … `content/results.txt` に統合

## 3. 検証状況（jsdomによるDOMテスト実施）
- 部員紹介：カード36枚＝写真img20＋人型SVG16、不正なsrcが0件（改善3の修正を確認）
- トップページ：ニュース最新5件（日付降順）・監督経歴3項目の描画を確認
- ニュースページ：6ヶ月フィルタで4件表示（2026-07-02時点）を確認
- 整理ページ：期限切れ1件（2025-11-10）の削除ブロック提示を確認
- 大会結果：content/results.txt から年度ボタン3つ（2026/2025/2024・降順）の自動生成を確認。日付ソートは単体テストで「新しい大会が先頭・日付なしは末尾」を確認
- JSファイル3本の構文チェックOK

---

## 4. 追加改修（2026年7月・表示改善）

トップ／ニュース／記録ページの表示品質に関する8件の改善を行った。
サイトの構成（8ページ・カラー・フォント・データ管理方式）は変更していない。

| No. | 種別 | 内容 | 対応ファイル |
|-----|------|------|--------------|
| 改善2-1 | ニュース一覧 | ニュース一覧ページ（/news/）を **1ページ10件** 表示にし、11件以上ある場合は **ページ送り（‹ 1 2 3 … ›）** を表示。ページ番号クリックで切り替え＋一覧先頭へスクロール | `js/content-loader.js`, `css/style.css` |
| 改善2-2 | ニュース | 表示日付が **本日から7日以内** のニュースに、金色の「**NEW**」バッジ（やわらかく点滅）を付与。**トップページ最新情報とニュース一覧ページの両方**に適用 | `js/content-loader.js`, `css/style.css` |
| 改善2-3 | 自己ベスト記録 | スマホなど画面が狭いときに「氏名（学年）」と「非表示」ボタンが不揃いに折り返す問題を修正。**氏名を左・ボタンを右**に固定し、全行を1行できれいに整列（`.player-cell` を flex 化） | `js/main.js`, `css/style.css` |
| 改善2-4 | ヘッダー | スクロール時にヘッダーが**透明化・文字色変化・ロゴ移動・高さ変化**して見栄えが乱れていた挙動を廃止。**常に紫の帯・同じ高さ・白文字で上部固定**とし、スクロール時のみ影をわずかに濃くする落ち着いた動作に変更 | `js/main.js`, `css/style.css` |
| 改善2-5 | 横スクロール | ページを右方向に少しスクロールできてしまう横あふれを修正（`body { overflow-x: clip }`。記録テーブル内の横スクロールは従来どおり維持） | `css/style.css` |
| 改善2-6 | 文字アニメ | デザインを崩さない範囲で、各ページ上部の見出し（ヒーロー／ページ見出し／記録・歴代の見出し）に**読み込み時のフェード＆ライズ**を追加。トップのスローガンにもスクロール表示（fade-up）を付与 | `css/style.css`, `index.html` |
| 改善2-7 | スマホ表示 | スマホでのニュース表示を整理（**日付＋カテゴリを1行目・タイトルを2行目**）。ページ送りボタンなどのタップ領域を44px以上に | `css/style.css` |
| 改善2-8 | 監督紹介 | 監督名の末尾「監督」を **サブ色（--sub #6B7280）・小さめ（15px）** で表示（氏名本体は24px） | `js/content-loader.js`, `css/style.css` |

### 実装メモ（更新担当者向け）
- **NEWバッジ**：判定は「ニュースの日付 ≥ 本日の7日前」。`content/news.txt` の日付だけで自動判定するため、**特別な操作は不要**。7日を過ぎると自動的に消える。
- **ページ送り**：ニュースが10件以下のときはボタン自体が表示されない（＝従来と同じ見た目）。11件目以降を書き足すと自動的にページ送りが出る。
- **ヘッダー**：スクロールで色や大きさが変わらなくなった。編集はこれまでどおり `js/layout.js`（見た目の帯色などは `css/style.css` の `.site-header`）。
- 「動きを減らす」設定の端末（OSのアクセシビリティ設定）では、NEWバッジの点滅と見出しアニメを自動的に停止する。

### 検証状況（Chromium ヘッドレスで確認）
- 横スクロール：トップ／ニュース／自己ベスト の各ページで、スマホ幅(390px)・PC幅(1280px)とも `documentのscrollWidth = 画面幅`（横あふれなし）を確認
- ヘッダー：スクロール前後で高さ60pxのまま・背景 紫(#6D28D9) のまま・`.is-scrolled` の付与のみを確認
- NEWバッジ：7日以内のテスト記事にのみ表示（トップ・一覧の両方）、点滅アニメ(newPulse)を確認
- ページ送り：17件時に1ページ目10件／2ページ目7件・ボタン「‹ 1 2 ›」を確認
- 監督ラベル：「監督」= color rgb(107,114,128)・15px、氏名本体24px を確認
- 自己ベスト表：`.player-cell` が display:flex・justify-content:space-between、ボタン11px を確認
- JS 3本の構文チェックOK

---

## 4. ヘッダー・ヒーロー改修（2026-07-07 確定分）

既存の開発要件書「4-1. ヘッダー」およびサイト構成書のヘッダー仕様を、以下の内容で更新する。

### 4-1. 確定した要件

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| H1 | ヘッダー | 帯の背景は**透明**。トップは写真の上、他ページは紫背景の上に浮かせる | ✅ |
| H2 | ヘッダー | ロゴは**白い角丸四角の塗り枠**（radius 14px / 余白 6px、CSS変数で調整可） | ✅ |
| H3 | ヘッダー | グローバルナビは**濃いめ・半透明の紫ピル** `rgba(75,20,145,0.55)`＋すりガラス風。文字は白、ホバーで文字が薄くなる `rgba(255,255,255,0.6)` | ✅ |
| H4 | ヘッダー | 現在ページの項目は濃い背景 `rgba(75,20,145,0.78)`。「記録」は子ページ閲覧中も濃い表示を維持 | ✅ |
| H5 | ヘッダー | ドロップダウン（記録の子メニュー）もナビと同じ紫ピル・白文字・ホバーで薄く。閲覧中の子項目にも濃い背景 | ✅ |
| H6 | ヘッダー | ロゴ＝左端／ナビ＝右端。**画面端から18px**（CSS変数 --header-edge-gap、15〜20pxで調整可） | ✅ |
| H7 | ヘッダー | ヘッダー帯は `pointer-events: none` で**クリック透過**。ロゴ〜ナビ間の空白は、下にスクロールしてきたページ内容（ニュースリンク等）をそのまま押せる。ロゴ・ナビ・ハンバーガー自体は押せる | ✅ |
| H8 | スマホ | ハンバーガーボタンにナビと同じ紫ピル背景（写真の明るい部分でも視認可能に） | ✅ |
| H9 | スマホ | メニュー展開中は**ロゴを非表示**（body.nav-open で制御）。×ボタンは残す | ✅ |
| H10 | スマホ | メニューは **top:0 から画面いっぱい**。背景は `rgba(69, 20, 160, 0.97)`（わずかに透け）。上端から下のコンテンツが見えない | ✅ |
| H11 | ヒーロー | テキストは下寄せにして上側に顔を見せる。PC: object-position center 22% / スマホ(〜767px): 28% / **大画面(1200px〜): 42%**（全員の顔が入る位置） | ✅ |

### 4-2. 実装箇所

- `css/style.css` … ヘッダー／ナビ／ドロップダウン／ハンバーガー／モバイルメニュー／ヒーローの各ブロック（すべて【調整】コメント付き）
- `js/layout.js` … ヘッダーのマークアップ（ロゴ・ナビ・ハンバーガー・モバイルメニュー、全ページ共通・1ファイル管理）
- `js/main.js` … メニュー開閉時の body.nav-open 付与、現在ページ判定（親ナビ＋ドロップダウン内リンク）

### 4-3. 検証済み項目（PC 1280/1920・スマホ390）

- 画面端の余白が両側とも18pxであること
- ドロップダウンが紫半透明・白文字・ホバーで文字が薄くなること
- ヘッダーの空白部分を通した実クリックで、下のニュースリンクから /records/results/ へ遷移できること
- モバイルメニューが画面全体（top 0〜bottom）を rgba(69,20,160,0.97) で覆い、上端から下が透けないこと
- メニュー展開中のロゴ非表示、×ボタンでの閉じ動作、メニューリンクからの遷移
- 1920px幅でヒーロー写真に全員の顔（写真内 427〜705px帯）が収まること

### 4-4. 追加要件（2026-07-07 追記分）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| H12 | 全ページ | 画面右下に「ページ上部へ戻る」ボタンを固定表示。ナビと同じ濃いめ・半透明の紫ピル＋白い上矢印。ホバーで矢印が薄くなり（rgba(255,255,255,0.6)）、ピルが少し浮く（translateY(-3px)）。クリックで最上部へスムーズスクロール。**300pxスクロールしたら出現**（main.js の定数 TOTOP_SHOW_AT で変更可、0で常時表示）。スマホの全画面メニュー展開中は非表示。サイズ48×48px・端から18px | ✅ |

- 実装箇所：`js/layout.js`（ボタン本体・全ページ共通）／`css/style.css`（.to-top）／`js/main.js`（セクション9：出現制御とスクロール）
- 検証済み：トップ・部員紹介・記録ページでの出現／非表示、ホバーの色と浮き、クリックで scrollY=0、メニュー展開中の非表示（PC 1280・スマホ390）

---

## 4. 2026-07-07 改修（第2回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加5 | サイト全体 | ホーム画面追加（Webアプリ化）時のアイコンを images/head/logo.png に設定。全12ページの head に apple-touch-icon と manifest.json（新規）を追加。派生アイコン icon-192.png / icon-512.png を生成 | ✅ |
| 修正2 | スマホ | 全画面メニュー下端の塗り残し（Chrome/Google アプリの下部ツールバー起因）を修正。.mobile-nav の高さを 100dvh 基準（100vh フォールバック付き）に変更 | ✅ |
| 修正3 | スマホ | リンク・ボタンが「2回タップしないと反応しない」問題を修正。全 :hover 装飾（34ルール）を @media (hover: hover) and (pointer: fine) でマウス環境限定にし、a / button に touch-action: manipulation を追加。キーボード用の :focus-within は全環境で有効のまま分離 | ✅ |
| 追加6 | 部員紹介 | members.txt に「役職：」欄を全36名分追加（現時点は全員空欄・後日記入）。記入がある部員のみ、カード右上＋拡大表示（モーダル）左上に金色バッジで役職を表示。空欄なら非表示 | ✅ |
| 安全1 | 安全面 | 連絡先メールの難読化。site.txt で「前半・後半」に分割記載し、実行時に JS が組み立てて表示。HTML・txt から完成形アドレスを除去（旧形式「連絡先メール：」も後方互換で動作） | ✅ |
| 安全2 | 安全面 | .git フォルダの公開防止。README に「公開時に .git を含めない」運用を追記し、netlify.toml に /.git/*・.DS_Store を外部から遮断する保険ルールを追加 | ✅ |
| 安全3 | 安全面（A） | アカウント防御の運用を明文化（Netlify・Google の2段階認証必須・権限の限定・卒業時引き継ぎ）。README・開発要件書に追記 | ✅ |
| 安全4 | 安全面（B） | 編集ミス耐性の強化。content/*.txt が読み込めない場合、該当セクションにのみ案内（.content-load-notice）を表示し、他セクション・ページ全体は巻き込まれない（runSafely によるセクション独立実行＋coach/members/ekiden/news の失敗時表示） | ✅ |

- 実装箇所：全HTML head／`manifest.json`（新規）／`css/style.css`（.mobile-nav 高さ・hover ガード・.member-role-badge・.content-load-notice・touch-action）／`js/content-loader.js`（役職・メール組み立て・showLoadNotice・runSafely）／`content/members.txt`（役職欄）／`content/site.txt`（メール分割）／`contact/index.html`（生アドレス除去）／`netlify.toml`（遮断ルール）
- 前提の整理（安全面）：本サイトは公開静的サイトのため、content/ に書いた内容は誰でも閲覧可能。**非公開にしたい情報はそもそも書かない**のが原則。訪問者がサイトを書き換えることはできず、書き換えられるのは Netlify 等のアカウント保有者のみ（＝アカウント防御が改ざん対策の本体）。

---

## 5. 2026-07-08 改修（第3回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善7 | 部員紹介 | 役職バッジをすりガラス風の金（半透明金グラデーション＋金縁＋背景ぼかし・文字は濃い琥珀色）へ変更し、サイズを拡大（カード14px／モーダル15px） | ✅ |
| 修正4 | 部員紹介 | モーダルの aria-hidden 警告を解消。開くと×ボタンへ、閉じるとクリック元カードへフォーカスを返してから aria-hidden を付与 | ✅ |
| 安全5 | 安全面 | 文字の選択コピーを不可に（user-select:none。フォーム入力欄・整理ページの .maint-block は除外）。※スクショ・ソース表示は防止不能 | ✅ |
| 追加7 | 全ページ | 起動スプラッシュを追加。セッション初回＋ヘッダーロゴタップ時に --primary-dark 全画面＋白角丸タイル上の logo.png＋光の輪（約1.4秒・SPLASH_TOTAL_MS で調整）。prefers-reduced-motion 時は非表示 | ✅ |
| 改善8 | アイコン | タブ・Android 用アイコンを角丸四角版（logo-rounded.png / icon-192 / icon-512）に差し替え。iPhone 用 apple-touch-icon は iOS 自動角丸のため正方形のまま（透過角は黒くなるのを回避） | ✅ |
| 修正5 | スマホ | 全画面メニュー下端の塗り残し再発を修正。高さを 100lvh 基準＋safe-area 下余白へ強化 | ✅ |
| 修正6 | スマホ | メニュー展開時にハンバーガー中央線が薄く残る問題を修正（visibility:hidden＋scaleX(0) 併用） | ✅ |
| 修正7 | 大会結果ほか | 選択中の年度ボタン／ブロックボタンの強調がスマホで消えていた問題を修正（前回の hover 一括変換で .active が hover 用メディアクエリに巻き込まれていたのが原因。分離して全端末で常時適用） | ✅ |
| 安全6 | 安全面 | 画像の保存抑止。右クリックメニュー・ドラッグ・iPhone長押し保存を不可に。※スクショ・URL直アクセスは防止不能 | ✅ |
| 追加8 | スケジュール | カレンダー下に「練習曜日」ボックスを追加。content/schedule.txt（新規）で タイトル／長距離／短距離／補足 を管理。行追加で項目増設可・補足空欄可 | ✅ |

- 実装箇所：`css/style.css`（バッジ・保護・スプラッシュ・メニュー高さ・active分離・練習曜日）／`js/layout.js`（④スプラッシュ）／`js/content-loader.js`（フォーカス管理・applyPracticeDays）／`js/main.js`（画像保護）／`content/schedule.txt`（新規）／`schedule/index.html`（ボックス設置）／`images/head/`（角丸アイコン）／全HTML（favicon 差し替え）

---

## 6. 2026-07-08 改修（第4回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 修正8 | ヘッダー | 「記録」ドロップダウンの背景が上のナビ（紫ピル）の背景と重なって二重に濃く見える問題を修正。ドロップダウンを親ピル下端より下げ（間隔 top:calc(100%+12px)＝実測7pxの隙間）、背景を分離。ホバーをつなぐ透明帯（.dropdown::before）を -18px/18px に拡大し、間隔を広げてもホバーが途切れないよう維持 | ✅ |

- 実装箇所：`css/style.css`（.dropdown の top、.dropdown::before の高さ）
- 検証：ヘッドレスChromiumで実測（ピル下端58.9px／ドロップダウン上端65.9px＝隙間7.0px）。ホバー前=hidden、記録ホバー中=visible、子項目へマウス移動後もvisible（橋が機能）を確認。ホバーのしやすさは従来どおり。

---

## 7. 2026-07-08 改修（第5回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善9 | 部員紹介（スマホ） | スマホ表示時の部員カードを「写真・氏名・学年・役職バッジ」のみのコンパクト表示に変更。得意種目／出身校／一言／「詳しく見る」はカード上では非表示（従来どおりカードをタップするとモーダルで全情報を表示）。並びは端末幅に応じて自動で2〜3列（`repeat(auto-fill, minmax(110px, 1fr))`、gap 12px）。標準幅スマホ(≈375px)＝2列、大型端末(≈430px)＝3列。アバター72px・氏名14px・学年11px・役職バッジ縮小へ調整 | ✅ |

- 実装箇所：`css/style.css`（`@media (max-width: 767px)` 内の `.members-grid` を1列固定→自動2〜3列へ変更し、`.member-event/.member-school/.member-comment/.member-more` を display:none、`.member-avatar/.member-name/.member-year/.member-role-badge` をコンパクト調整）
- 変更なし：HTML・JS（モーダル/タップ挙動は既存実装をそのまま使用）。PC・タブレット表示は従来どおり（PC3列／タブレット2列・フル情報）。
- 列数の調整：`minmax(110px, 1fr)` の 110px を小さくすると列が増え、大きくすると減る。
- 検証：実カード構造でのプレビュー（`preview-members-mobile.html`）で375px=2列・430px=3列、非表示項目・役職バッジ位置を目視確認。

---

## 8. 2026-07-10 改修（第6回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加9 | ニュース記事 | ニュース記事ページの**画像レイアウト方針**を定義。メイン画像の「向き」と「枚数」で表示形式を自動的に選ぶ。①**縦長メイン2枚**＝中央揃えのグリッド表示（記事ページ内スコープCSSで最大幅を抑えて中央寄せ・写真が大きくなりすぎないよう配慮）②**メイン3枚以上、または横長メイン2枚以上**＝カルーセル表示。グリッドは写真サイズを考慮して中央表示とする | ✅（①実装／②方針確定・未実装） |

- **適用第1号**：`news/2026-01-11-kasugaimarason/index.html`（第44回新春春日井マラソン結果報告）。縦長メイン2枚（表彰／結果）を中央グリッドで表示。
- **実装箇所**：記事ページ内の `<style>`（クラス `.main-portrait-grid`）。**グローバルの `css/style.css`・`js/main.js` には手を加えない**、記事単位のスコープ実装とした。`max-width`（縦長の最大横幅）／`gap`（画像間隔）に【調整】コメントを付与。スマホでも2枚並びを維持。
- **カルーセル（②）は未実装**：現時点で該当記事（3枚以上・横長2枚以上）がないため。**実際に必要になった記事の回**で、記事ページ内に独立実装（CSS＋最小限のJS。左右スワイプ／インジケータ等）し、その仕様を本まとめ・構成書 5-10・要件書 7-6 へ追記する。
- 判定ルールまとめ：縦長×2 → 中央グリッド／メイン≥3 → カルーセル／横長×2以上 → カルーセル。
- **検証**：画像実体はサンドボックスで検証不可のため、ローカル（Live Server）または Netlify での**目視確認を推奨**。番号付きリスト等を含む記事の見え方も同様に本番相当環境で確認する。

---

## 9. 2026-07-10 改修（第7回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加10 | ニュース記事 | 第6回で方針のみ確定していた**カルーセル表示を初回実装**（横長メイン2枚以上／メイン3枚以上のとき）。記事ページ内に完結する独立実装とし、**グローバルの `css/style.css`・`js/main.js` は変更しない**。操作は「左右矢印ボタン／ドットインジケータ／スワイプ／キーボード←→」。 | ✅（実装） |

- **適用第1号**：`news/2026-01-18-okazaikisiminekiden/index.html`（第77回岡崎市民駅伝競走大会 結果報告）。横長メイン2枚（結果／結果）をカルーセルで表示。
- **実装方式**：記事ページ内の `<style>`＋`<script>`（IIFEで閉じる）。クラスは衝突回避のため `.news-carousel` / `.nc-*` に統一。
  - 構造：`.news-carousel > .nc-viewport(.nc-track > .nc-slide) + 矢印 .nc-prev/.nc-next` ／ `.nc-cap`（スライド連動キャプション）／ `.nc-dots > .nc-dot`。
  - 動作：`translateX(-idx*100%)` でスライド、ドット `.is-active` 同期、キャプションを配列 `caps[]` から差し替え。スワイプは閾値40px。
  - 見た目：中央寄せ（`margin:0 auto`）、最大幅は CSS変数 `--nc-max`（既定720px・【調整】）、角丸10px、矢印は紫ピル同系色でタップ領域44px、ドットは現在位置が紫（#6D28D9）。
  - 配慮：`prefers-reduced-motion` でトランジション無効化、`aria-roledescription="カルーセル"`・`aria-live` キャプション・矢印/ドットに `aria-label`、`tabindex="0"` でキーボード操作可。
- **判定ルール（再掲）**：縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`）。
- **今後**：3枚以上のカルーセルも同一実装で対応可（`caps[]` を枚数分にし、`.nc-slide` を追加するだけ）。写真の縦横比が大きく異なる場合は表示高さがスライド最大に揃う点に留意。
- **検証**：画像実体はサンドボックスで確認不可。矢印/ドット/スワイプ/キーボードの動作と表示は Live Server または Netlify で目視確認する。

---

## 10. 2026-07-10 改修（第8回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善11 | ニュース記事（カルーセル） | カルーセルの**操作ボタン（矢印・ドット）を写真の下の操作バー `.nc-controls` にまとめ、写真には一切重ねない**方式へ変更（見やすさの改善）。矢印は写真上の絶対配置をやめ、`矢印 ─ ドット ─ 矢印` を中央に並べる操作バーに集約。`z-index`・`backdrop-filter` は不要になり除去。 | ✅ |
| 追加12 | ニュース記事（ギャラリー） | 記事末尾の**複数枚ギャラリー**（「最後に画像グリッド」）を、向き混在でも比率を保ったまま隙間なく詰める**中央寄せメーソンリー**（CSS `columns`）で実装。トリミング・変形なし。クラス `.news-gallery`（記事内スコープ）。 | ✅ |

- **カルーセル改修の適用**：`news/2026-01-18-okazaikisiminekiden/index.html`（岡崎駅伝）に反映済み。JSはクラス名で要素取得しているため、HTML構造変更後もそのまま動作（`.nc-prev/.nc-next/.nc-dot/.nc-cap/.nc-viewport/.nc-track`）。
- **カルーセル構造（改定後）**：`.news-carousel > .nc-viewport(.nc-track>.nc-slide) ＋ .nc-cap ＋ .nc-controls(.nc-prev / .nc-dots / .nc-next)`。矢印は44px、ドットは当たり判定拡大（`::before`）、現在位置＝紫。
- **ギャラリー適用**：`news/2026-02-01-biwakokurokan/index.html`（びわ湖クロカン）。単一メイン画像（集合写真）＋末尾8枚をメーソンリー表示。`columns:3 220px`（PC最大3列）／スマホ `columns:2 140px`、中央寄せ（`max-width var(--gallery-max)`・`margin:auto`）、`break-inside:avoid`。列ごとに上から詰めるため写真は「左上→下→次列」の順に並ぶ。左右順を厳密にしたい場合は均一トリミングの格子型に切替可。
- **判定ルール（再掲）**：単一メイン＝標準の大きめ表示／縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`、操作バーは写真の下）。末尾の複数枚は `.news-gallery`（メーソンリー）。
- **検証**：画像実体はサンドボックスで確認不可。カルーセルの操作バー・ギャラリーの詰まり方は Live Server／Netlify で目視確認。

---

## 11. 2026-07-10 改修（第9回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加13 | ニュース記事（動画） | ニュース記事で**動画（`<video>`／.mp4）に初対応**。メインが動画3本以上（または動画を並べる場合）は、既存のカルーセルと同一構造・同一操作で、スライドの中身を `<img>` から `<video>` に置き換えた**動画カルーセル**として表示する。記事ページ内に完結（グローバルCSS/JSは変更しない）。 | ✅ |

- **適用第1号**：`news/2026-02-15-aitiikeekiden/index.html`（第9回愛知池駅伝 結果報告）。動画3本（`2026-02-15-1〜3.mp4`）をカルーセルで表示。
- **動画の既定設定**：`controls`（再生ボタン）／`playsinline`／`preload="metadata"`／`controlslist="nodownload"`、**自動再生なし**。**スライド切替時に表示外の動画を自動 `pause()`**（音が残らないように）。
- **見た目・操作**：写真カルーセルと共通（`.news-carousel` / `.nc-*`、操作バー `.nc-controls` は動画の下・重ねない）。キャプション欄 `.nc-cap` は動画番号「N / 3」を表示。動画の最大高さは CSS変数 `--nc-vmaxh`（既定70vh・【調整】）で縦長動画が大きくなりすぎないよう制限、`.nc-viewport` 背景は黒（縦長時の余白対策）。スワイプは「横移動が縦移動より大きいときのみ」切替（動画のシークバー操作との競合回避）。
- **運用上の注意**：動画ファイルは容量が大きく Netlify の帯域を消費するため、事前に圧縮すること。必要に応じて `poster` 画像（サムネイル）を各 `<video>` に追加可（未指定時は先頭フレーム/黒画面）。
- **判定ルール（更新）**：単一メイン＝標準表示／縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`）。**メインが動画のカルーセルは同構造で `<video>` を使用**。末尾の複数枚（静止画）は `.news-gallery`（メーソンリー）。
- **検証**：動画実体はサンドボックスで再生確認不可。再生・切替時の自動停止・スワイプ・操作バーは Live Server／Netlify で目視確認する。

---

## 12. 2026-07-10 改修（第10回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善12 | ニュース記事（カルーセル） | **画像カルーセルを4枚・キャプション付きで初適用**（従来は横長2枚）。あわせて写真が縦横混在でも見やすいよう、**写真を中央寄せ＋最大高さ制限**（CSS変数 `--nc-maxh` 既定70vh）を画像カルーセルにも追加（縦長写真が大きくなりすぎない）。 | ✅ |

- **適用例**：`news/2026-03-13-sasiire/index.html`（新入生からの差し入れ）。写真4枚（`2026-03-13-1〜4.jpg`）を、キャプション（中村くん／伊藤くん／小栗くん／宮田くん）付きカルーセルで表示。
- **拡張の要点**：枚数は `.nc-slide`・`.nc-dot`・キャプション配列 `caps[]` を枚数分にするだけ（今回は4）。`.nc-slide` を `display:flex;align-items:center;justify-content:center` にし、`.nc-img{ max-width:100%; max-height:var(--nc-maxh) }` で縦長も中央に収める。操作バーは従来どおり写真の下（重ねない）。
- **判定ルール（再掲）**：単一メイン＝標準表示／縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`、写真は中央寄せ＋`--nc-maxh` で高さ制限、動画は `<video>`）。末尾の複数枚（静止画）は `.news-gallery`（メーソンリー）。
- **検証**：画像実体はサンドボックスで確認不可。4枚の切替・キャプション連動・縦長時の収まりは Live Server／Netlify で目視確認。

---

## 13. 2026-07-15 改修（第11回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 改善13 | ニュース記事（カルーセル） | **1つの記事ページに複数のカルーセルを設置できるよう対応**。記事内JSを「最初の1つだけ初期化」から**全 `.news-carousel` を個別に初期化**する方式へ変更（`querySelectorAll` ＋ 各カルーセルごとにクロージャで状態を保持）。写真用・動画用が同一ページに共存可能。 | ✅ |
| 追加14 | ニュース記事（動画） | **記事末尾に動画カルーセルを配置するパターン**を追加（従来の末尾は静止画メーソンリー `.news-gallery` のみ）。動画カルーセルには `.is-video` を付与し、`.nc-viewport` 背景を黒にする（縦長動画の余白対策）。 | ✅ |
| 追加15 | SEO | 記事追加に伴い **`sitemap.xml` に新記事URLを1行追加**（20→21URL）。記事ページの `<head>` に **canonical**（`https://ait-athletics.github.io/news/フォルダ名/`）を設定。 | ✅ |

- **適用例**：`news/2026-03-15-niigatahahumarason/index.html`（新潟ハーフマラソン2026 結果報告）。メイン＝写真3枚のカルーセル（`2026-03-15-3/2/1.jpg` の順）＋記事末尾＝動画3本のカルーセル（`2026-03-15-4〜6.mp4`）。
- **実装の要点**：CSSは1セット（`.news-carousel` / `.nc-*`）を共有し、写真は `--nc-maxh`、動画は `--nc-vmaxh` で最大高さを制御。動画カルーセルのみ `.is-video` で黒背景。操作バー `.nc-controls` は従来どおり写真・動画の下（重ねない）。JS は各カルーセルを独立に初期化するため、キャプション「N / 件数」・ドット・スワイプ・キーボードが個別に動作する。
- **判定ルール（再掲・更新）**：単一メイン＝標準表示／縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`）。動画は同構造で `<video>`（`.is-video`）。末尾の複数枚（静止画）は `.news-gallery`（メーソンリー）、末尾の動画は動画カルーセル。
- **記事追加時のSEO作業（定型）**：`sitemap.xml` にその記事のURLを1行追加し、記事の canonical をフォルダ名に合わせる。
- **検証**：`sitemap.xml` のXML妥当性・URL重複なし・21URLを確認。画像/動画の実体はサンドボックスで確認不可のため、2つのカルーセルの独立動作は Live Server／Netlify で目視確認する。

---

## 14. 2026-07-15 改修（第12回）

| No. | 種別 | 内容 | 対応 |
|-----|------|------|------|
| 追加16 | ニュース記事（動画） | **メイン動画が1本だけの場合の単体表示**を追加（従来の動画対応はカルーセルのみ）。カルーセルを使わず、中央寄せ・黒背景・最大高さ制限の枠 `.news-video-single` に `<video>` を1本置く。動画の設定は既存方針と共通（`controls`／自動再生なし／`playsinline`／`preload="metadata"`／`controlslist="nodownload"`）。 | ✅ |
| 追加17 | SEO | 記事追加に伴い `sitemap.xml` を更新（22→23URL）。記事の canonical を設定。 | ✅ |

- **適用第1号**：`news/2026-03-28-toyotaboushokukirokukai/index.html`（第2回トヨタ紡織記録挑戦会 結果報告）。メイン動画1本（`2026-03-28-1.mp4`）を単体表示。
- **実装箇所**：記事ページ内の `<style>`（`.news-video-single`）のみ。JSは不要（カルーセルではないため）。【調整】は `--nv-max`（最大幅・既定720px）／`--nv-maxh`（最大高さ・既定70vh）。
- **判定ルール（更新・現行の全パターン）**：
  - 静止画：単一メイン＝標準表示（`article-img-main`）／縦長メイン2枚＝中央グリッド（`.main-portrait-grid`）／メイン3枚以上・横長2枚以上＝カルーセル（`.news-carousel`、写真は `--nc-maxh` で高さ制限）
  - 動画：**1本＝単体表示（`.news-video-single`）** ／ 2本以上＝カルーセル（`.news-carousel.is-video`、`<video>`・`--nc-vmaxh`）
  - 記事末尾：複数の静止画＝メーソンリー（`.news-gallery`）／複数の動画＝動画カルーセル
  - 1記事に複数カルーセルを設置可（記事内JSが全 `.news-carousel` を個別に初期化）
- **検証**：`sitemap.xml` のXML妥当性・重複なし・23URLを確認。動画の再生はサンドボックスで確認不可のため Live Server／Netlify で目視確認する。
